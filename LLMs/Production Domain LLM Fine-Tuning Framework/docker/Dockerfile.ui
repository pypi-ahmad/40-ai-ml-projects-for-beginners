FROM python:3.12-slim

WORKDIR /app
COPY pyproject.toml uv.lock* README.md ./
COPY src ./src
COPY configs ./configs

RUN pip install --no-cache-dir uv && uv sync --frozen || uv sync

EXPOSE 8501
CMD ["uv", "run", "domain-llm", "serve-ui", "--port", "8501"]
